# ─── Python ───────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/
build/
dist/

# ─── Env / secrets ────────────────────────────────────────
.env
.env.*
*.key

# ─── OS / editor ──────────────────────────────────────────
.DS_Store
.idea/
.vscode/
.pi/

# ─── Stray binaries / event handouts ──────────────────────
*.pdf

# ─── Local-only notes (meta, other tracks, research, handouts) ───
# All off-submission material now lives under _local/ (see _local/INDEX.md).
_local/

# Project config — kept at repo root by convention (tools expect it), local-only.
CLAUDE.md

# Infineon / Karpathy track code — left at root; tracked docs reference this path.
karpathy-experiment/

# Stray / unrelated experiments
tictactoe-extension/

# React capture app (Node)
webapp/node_modules/
webapp/dist/

# Research run artifacts (raw generated sessions; reports promoted manually)
research/runs/

# eval reports (data; promote notable ones manually)
evals/reports/
research/runs/_global_best.json
