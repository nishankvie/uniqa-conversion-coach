# ─── Python ───────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/
build/
dist/

# ─── Env / secrets ────────────────────────────────────────
.env
.env.*
*.key

# ─── OS / editor ──────────────────────────────────────────
.DS_Store
.idea/
.vscode/
.pi/

# ─── Stray binaries / event handouts ──────────────────────
*.pdf

# ─── Local-only notes (meta, other tracks, research, handouts) ───
# All off-submission material now lives under _local/ (see _local/INDEX.md).
_local/

# Project config — kept at repo root by convention (tools expect it), local-only.
CLAUDE.md

# Infineon / Karpathy track code — left at root; tracked docs reference this path.
karpathy-experiment/

# Stray / unrelated experiments
tictactoe-extension/

# Demo React app (Node)
demo/node_modules/
demo/dist/

# Research run artifacts (raw generated sessions; reports promoted manually)
research/runs/

# eval reports (data; promote notable ones manually)
evaluations/reports/
research/runs/_global_best.json

# ─── Heavy generated data — regenerable, never in git (233MB) ───
# Persona SFT datasets + soft labels (regenerate via research/datagen_v2.py)
datasets/
# Trained sklearn persona-predictor blobs (regenerate via research/train_persona_predictor.py)
models/
# Slurm training shards + cluster-produced LoRA adapters
slurm/data/
slurm/data_tagged/
slurm/out/
