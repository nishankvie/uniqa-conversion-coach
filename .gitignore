# ─── Python ───────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/
build/
dist/

# ─── Env / secrets ────────────────────────────────────────
.env
.env.*
*.key

# ─── OS / editor ──────────────────────────────────────────
.DS_Store
.idea/
.vscode/
.pi/

# ─── Stray binaries / event handouts ──────────────────────
*.pdf

# ─── Local-only / off-submission material ───
# Everything not part of the UNIQA submission lives under _local/ (hidden, never tracked):
# other hackathon tracks (_local/other-tracks/), meta notes, handouts, source decks.
_local/

# Project config — kept at repo root by convention (tools expect it), local-only.
CLAUDE.md

# Demo React app (Node)
demo/node_modules/
demo/dist/

# Research run artifacts (raw generated sessions; reports promoted manually)
research/runs/

# eval reports (data; promote notable ones manually)
evaluations/reports/
research/runs/_global_best.json

# ─── Heavy generated data — regenerable, never in git (233MB) ───
# Persona SFT datasets + soft labels (regenerate via research/datagen_v2.py)
datasets/
# Trained sklearn persona-predictor blobs (regenerate via research/train_persona_predictor.py)
models/
# Slurm training shards + cluster-produced LoRA adapters
slurm/data/
slurm/data_tagged/
slurm/out/

# sim_loop raw session output (regenerable; the demo copy lives in sim_loop/replay/public/)
sim_loop/out/
